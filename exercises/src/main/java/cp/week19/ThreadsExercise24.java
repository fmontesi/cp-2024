package cp.week19;

/**
 *
 * @author Fabrizio Montesi <fmontesi@imada.sdu.dk>
 */
public class ThreadsExercise24
{
	/*
	* Recursively visit (walk) the 'data' directory to find all regular files.
	* For each, create a virtual thread that counts how many times the letter 'a' appears in the file.
	* Whenever a thread terminates, print on screen the name of the file and its corresponding result (the number of occurrences of 'a').
	*/
}
